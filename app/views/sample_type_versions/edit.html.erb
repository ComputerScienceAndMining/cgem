<%= show_error_messages!(@sample_type_version) %>

<div class="container">
  <ol class="breadcrumb">
    <li>
      <% if policy(SampleTypeVersion).index? %>
        <%= link_to SampleTypeVersion.model_name.human(count: 2), sample_type_versions_path %>
      <% else %>
        <%= SampleTypeVersion.model_name.human(count: 2) %>
      <% end -%>
    </li>
    <li>
      <% if policy(@sample_type_version).show? %>
        <%= link_to @sample_type_version.to_s, sample_type_version_path(@sample_type_version) %></li>
      <% else %>
        <%= @sample_type_version.to_s %></li>
      <% end -%>
    <li class="active"><%= t('edit', scope: :breadcrumb) %></li>
  </ol>
</div>

<div class="form-wrapper" ng-controller="SampleTypeVersionsEditCtrl as editCtrl">
  <%= render 'form' %>
</div>
