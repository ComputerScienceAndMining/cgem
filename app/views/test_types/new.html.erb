<%= show_error_messages!(@test_type) %>

<div class="container"<%# ng-controller="test_typesNewCtrl" %>>
  <ol class="breadcrumb">
    <li>
      <% if policy(TestType).index? %>
        <%= link_to TestType.model_name.human(count: 2), test_types_path %>
      <% else %>
        <%= TestType.model_name.human(count: 2) %>
      <% end -%>
    </li>
    <li class="active"><%= t('new', scope: :breadcrumb) %></li>
  </ol>
</div>

<div class="form-wrapper" ng-controller="TestTypesNewCtrl as newCtrl">
  <%= render 'form' %>
</div>
